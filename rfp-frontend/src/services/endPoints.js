//Keep all API endpoints here so that future changes to the endpoints won't affect the services.
export const END_POINTS = {
  // Authentication endpoints
  googleAuth: '/api/auth/google',
  getMe: '/api/me',
  health: '/api/health',

  // Upload endpoints
  uploadDocuments: '/api/upload',

  // Search endpoints
  search: '/api/search',
};
